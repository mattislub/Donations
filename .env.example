# Server port
PORT=3001

# Preferred: single connection string
DATABASE_URL=postgres://user:password@localhost:5432/donations

# Or use individual PG* variables
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=donations

# SMTP configuration
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_username
SMTP_PASS=your_password
SMTP_FROM="Beit Tefilah Givat Ze'ev <no-reply@example.com>"
SMTP_TO=info@example.com
